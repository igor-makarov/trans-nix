amends "package://github.com/jdx/hk/releases/download/v1.18.1/hk@1.18.1#/Config.pkl"
import "package://github.com/jdx/hk/releases/download/v1.18.1/hk@1.18.1#/Builtins.pkl"

local linters = new Mapping<String, Step> {
    ["lua-check"] {
        glob = "**/*.lua"
        check = "lua-language-server --check ."
    }
    ["stylua"] {
        glob = "**/*.lua"
        check = "stylua --check {{files}}"
        fix = "stylua {{files}}"
    }
    ["actionlint"] {
        glob = ".github/workflows/*.{yml,yaml}"
        check = "actionlint {{files}}"
    }
    ["ruff-check"] {
        glob = "{bin/trans-nix,tests/**/*.py}"
        check = "ruff check bin/trans-nix tests/"
        fix = "ruff check --fix bin/trans-nix tests/"
    }
    ["ruff-format"] {
        glob = "{bin/trans-nix,tests/**/*.py}"
        check = "ruff format --check bin/trans-nix tests/"
        fix = "ruff format bin/trans-nix tests/"
    }
    ["shellcheck"] {
        glob = "**/*.sh"
        check = "shellcheck {{files}}"
    }
}

hooks {
    ["pre-commit"] {
        fix = true
        stash = "git"
        steps = linters
    }
    ["fix"] {
        fix = true
        steps = linters
    }
    ["check"] {
        steps = linters
    }
}
